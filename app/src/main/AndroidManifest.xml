<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hoa.hoatranreportbasic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MyService" />

        <!-- [Broadcast Receiver use fiel AndroidManifest.xml -->
        <!-- //Xin quyền truy cập trạng thái của... -->
        <!-- uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/ -->
        <!-- //Đăng ký sự kiện lắng nghe khi thay đổi mạng hoặc Wifi -->
        <!--
            receiver android:name=".NetworkChangeReceiverUseManifest" >
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" /
        -->
        <!-- //lắng nghe bật tắt mạng và wifi -->
        <!--
        /intent-filter>
        </receiver
        -->
        <!-- Broadcast Receiver use fiel AndroidManifest.xml] -->


        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".FragmentTransitionAndServiceActivity"
            android:label="@string/title_activity_fragment_transition_and_service"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BindService"></activity>
    </application>

</manifest>